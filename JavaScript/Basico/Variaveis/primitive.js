//Tipo primitivos (imutáveis) - string, number, boolean, undefined, null (bigint, symbol)
const name = "John"; // string, textos
const age = 25; // number, numeros
const hasHobbies = true; // boolean, true ou false
const lastName = null; // null, nao aponta para nenhum local na memoria
const height = undefined; // undefined, nao aponta para nenhum local na memoria
const weight = 80.5; // number (float), nao existe o tipo float no JS


